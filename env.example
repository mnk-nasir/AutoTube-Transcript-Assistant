# Google API key for the Generative Language endpoint
GOOGLE_API_KEY=your_google_api_key_here

# Default model used when not provided on the command line
DEFAULT_MODEL=gemini-1.5-flash

# Optional defaults for quick testing
YOUTUBE_URL=https://www.youtube.com/watch?v=Ovg_KfKxnC8
PROMPT_TYPE=transcript

# Where outputs will be saved if --save is used
OUTPUT_DIR=outputs
